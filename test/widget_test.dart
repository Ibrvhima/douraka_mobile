// Tests de base DouraKa
// Les écrans nécessitent un token JWT + un backend actif,
// donc les tests d'intégration sont gérés en dehors du widget test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — projet DouraKa configuré', () {
    expect(true, isTrue);
  });
}
